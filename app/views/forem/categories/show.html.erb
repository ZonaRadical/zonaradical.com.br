<div class='category'>
  <%= content_for :for_head do %>
    <title> <%= @category.name.to_s %> | Forum Zona Radical</title>
  <% end %>
  <h1>  <%= link_to t('forem.forum.forums'), forem.forums_path %> &raquo;
   <!-- <%= link_to forem_emojify(@category), [forem, @category] %> -->
  </h1>
  <%= render @category %>
</div>