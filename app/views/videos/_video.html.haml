%li
  .video-thumbnail[video]
    %p.video-thumbnail--name=link_to video.title, show_video_path(video.video_category, video)
    .video-thumbnail--image= link_to thumbnail(video.source_link), show_video_path(video.video_category, video)
  -if can? :video, :update
    = link_to 'Edit', edit_video_path(video)
  -if can? :video, :delete
    = link_to 'Destroy', video, method: :delete, data: { confirm: 'Are you sure?' }
  .clear
